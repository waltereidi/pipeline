name: Deploy .NET App to Amazon EC2

on:
  push:
    branches:
      - main  # dispara o deploy quando há push na branch main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4

      - name: OpenCommit — improve commits with AI 🧙
        uses: di-sukharev/opencommit@github-action-v1.0.2
        with:
            key: ${{ secrets.OCO_OPENAI_API_KEY }}
